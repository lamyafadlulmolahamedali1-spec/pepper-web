name: pepper_clinical_v6
description: Pepper Clinical Infinity V6 — secure mobile client. (c) 2026 Lamya F. H. Ali.
publish_to: 'none'
version: 6.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  flutter_secure_storage: ^9.2.2
  camera: ^0.11.0+2
  google_mlkit_pose_detection: ^0.12.0
  google_mlkit_face_detection: ^0.12.0
  fl_chart: ^0.69.2
  url_launcher: ^6.3.1
  shared_preferences: ^2.3.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
